<CycloneDDS xmlns="https://cdds.io/config"
             xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
             xsi:schemaLocation="https://cdds.io/config https://raw.githubusercontent.com/eclipse-cyclonedds/cyclonedds/master/etc/cyclonedds.xsd">
  <Domain id="0">
    <General>
      <!-- Robot DDS stays on the wired robot subnet. Tailscale carries only the
           Foxglove WebSocket, not DDS multicast or its high-rate datagrams. -->
      <Interfaces><NetworkInterface name="eth0"/></Interfaces>
      <AllowMulticast>true</AllowMulticast>
      <FragmentSize>1200B</FragmentSize>
    </General>
    <Discovery>
      <ParticipantIndex>auto</ParticipantIndex>
      <MaxAutoParticipantIndex>30</MaxAutoParticipantIndex>
    </Discovery>
    <Internal>
      <!-- Do not require a large host receive buffer here. Stock Ubuntu and
           Jetson installations otherwise reject the DDS participant at start. -->
      <Watermarks><WhcHigh>500kB</WhcHigh></Watermarks>
    </Internal>
    <!-- The gateway container is read-only, so DDS diagnostics belong in the
         container log rather than a cyclonedds.log file in the workspace. -->
    <Tracing>
      <Verbosity>severe</Verbosity>
      <OutputFile>stderr</OutputFile>
    </Tracing>
  </Domain>
</CycloneDDS>
